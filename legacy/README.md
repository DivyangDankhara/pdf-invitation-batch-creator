# Legacy Prototype Scripts

These files are the original one-off Python scripts kept for reference only.

The current product does not import or execute them. Use the React + FastAPI app from the repository root instead:

```powershell
.\run.ps1
```
